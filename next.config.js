/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tiendaliving.com.ar','127.0.0.1', 'https://berapositivo.onrender.com/'],
      },
}

module.exports = nextConfig
